# 在 Blender 中标记顶点以绘制线条

## 步骤说明

### 1. 选择 layer1 对象
- 在 Blender 中选择名为 `layer1` 的 mesh 对象

### 2. 进入编辑模式
- 按 `Tab` 键或点击左上角的 "Edit Mode" 按钮

### 3. 切换到顶点选择模式
- 按 `1` 键（数字键盘）或点击工具栏中的顶点选择图标

### 4. 创建顶点颜色属性
- 切换到 "Vertex Paint" 模式（按 `Ctrl+Tab` 或点击模式选择器）
- 或者直接在 "Edit Mode" 中：
  - 在右侧属性面板（按 `N` 键）找到 "Mesh Data" 标签
  - 展开 "Vertex Colors" 部分
  - 点击 "+" 按钮创建一个新的顶点颜色属性
  - 命名为 "Col"（默认名称即可）

### 5. 标记需要画线的顶点
- 选择你想要画线的顶点（按住 `Shift` 可以多选）
- 切换到 "Vertex Paint" 模式（`Ctrl+Tab`）
- 在左侧工具栏选择画笔工具
- 设置画笔颜色：
  - **R (红色)**: 设置为 1.0 或接近 1.0（这是标记的关键）
  - **G (绿色)**: 用于区分不同的线段组（相同G值的顶点会被连接）
  - **B (蓝色)**: 用于区分不同的线段组（相同B值的顶点会被连接）
- 在选中的顶点上绘制颜色

### 6. 标记规则说明

代码会识别满足以下条件的顶点：
- **R 通道 > 0.5**：顶点被标记为需要画线
- **G 和 B 通道**：用于分组，相同 G 和 B 值的顶点会被分在同一组
- **连接方式**：同一组内的顶点会按顺序连接（v0→v1→v2→...）

### 7. 示例

如果你想画一条线，需要：
1. 选择这条线上的所有顶点
2. 给它们设置相同的颜色：
   - R = 1.0（标记为需要画线）
   - G = 0.0, B = 0.0（第一组）
   - 或者 G = 0.5, B = 0.0（第二组，如果有多条线）

如果你想画多条独立的线：
- 第一条线：R=1.0, G=0.0, B=0.0
- 第二条线：R=1.0, G=0.5, B=0.0
- 第三条线：R=1.0, G=0.0, B=0.5
- 等等...

### 8. 导出 GLB
- 确保顶点颜色已保存
- 导出为 GLB 格式（File → Export → glTF 2.0 (.glb/.gltf)）
- 确保勾选 "Include Vertex Colors"

## 注意事项

1. **顶点顺序很重要**：同一组内的顶点会按它们在数组中的顺序连接。如果需要特定的连接顺序，可以在 Blender 中调整顶点的顺序。

2. **颜色精度**：代码使用 `Math.round(color * 10)` 来分组，所以颜色值的小数点后第一位精度会被保留。例如：
   - G=0.0, B=0.0 和 G=0.05, B=0.0 会被分在同一组
   - G=0.0, B=0.0 和 G=0.15, B=0.0 会被分在不同组

3. **调试**：打开浏览器控制台，你会看到类似这样的日志：
   - `Layer1 找到 X 个标记的顶点`
   - `Layer1 创建了 X 条自定义线条`

如果看不到这些日志，说明：
- 顶点颜色没有正确导出
- 或者 R 通道值不够高（尝试设置为 1.0）

